{
  "name": "estadisticas-honorlab",
  "version": "1.0.0",
  "private": true,
  "description": "Tablero de avance de ventas y proyeccion de cierre mensual (estadisticas.honorlab.dev)",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server start",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "ingest": "npm --prefix server run ingest --",
    "check": "node --experimental-strip-types --check server/src/index.js"
  },
  "engines": { "node": ">=20" }
}
